package view;

import javax.swing.*;

public class CurrentMealPlanPanel extends JPanel {
    public CurrentMealPlanPanel() {
        JLabel label = new JLabel("Current Meal Plan");
        add(label);
    }
}
